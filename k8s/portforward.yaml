apiVersion: v1
kind: Service
metadata:
  name: ollamaext
spec:
  type: NodePort
  selector:
    app: ollama
  ports:
    - protocol: TCP
      port: 80        # Service port
      targetPort: 3000 # Pod port
      nodePort: 30000  # The port exposed on nodes (automatically chosen if omitted)

