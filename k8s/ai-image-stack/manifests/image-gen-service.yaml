apiVersion: v1
kind: Service
metadata:
  name: ai-image-gen-ui
  labels:
    app: ai-image-gen-ui
spec:
  type: ClusterIP
  selector:
    app: ai-image-gen-ui
  ports:
    - name: http
      port: 80
      targetPort: http
